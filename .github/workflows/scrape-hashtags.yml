name: Update TikTok Hashtags Weekly

on:
  schedule:
    - cron: '0 3 * * 1'  # Every Monday at 3 AM UTC
  workflow_dispatch:  # Allows manual triggering

permissions:
  contents: write  # IMPORTANT: Allows pushing to repo

jobs:
  scrape-and-update:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        pip install pandas openpyxl requests
    
    - name: Run hashtag scraper
      run: |
        python tiktok_scraper_simple.py
        echo "✅ Scraper completed"
    
    - name: Verify file was created
      run: |
        echo "Looking for Excel files..."
        ls -lah *.xlsx || echo "❌ No Excel files found!"
    
    - name: Commit and push updated Excel file
      run: |
        # Configure git
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        
        # Add the file explicitly
        git add -f *.xlsx
        
        # Show what's staged
        echo "Staged files:"
        git diff --staged --name-only
        
        # Commit and push
        git commit -m "Update hashtags - $(date +'%Y-%m-%d')" && git push || echo "No changes or push failed"
    
    - name: Verify file in repo
      run: |
        git pull
        echo "Files in repo after push:"
        ls -lah *.xlsx || echo "Still no files!"
